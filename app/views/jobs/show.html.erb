<h4>Recruiter > Jobs > Job</h4>
<table border=1>
<tr>
<th>Job Title</th>
<th>Job Description</td>
<th>Location</td>
<th>Action</td>
<tr>
<tr>
<td><%= @job.title %></td>
<td><%= @job.description %></td>
<td><%= @job.location %></td>
<td><%= link_to "Edit", edit_job_path(@job) %> | <%= link_to "Destroy", job_path(@job),
method: :delete,
data: { confirm: "Are you sure?" } %></td>
</tr>
</table>
<div><%= link_to "back", jobs_path %></div>
